# v3.800.14
# https://virtocommerce.atlassian.net/browse/VCST-3064
name: Release

on:
  workflow_dispatch:

jobs:
  release:
    uses: VirtoCommerce/.github/.github/workflows/release.yml@v3.800.14    
    secrets:
      envPAT: ${{ secrets.REPO_TOKEN }}